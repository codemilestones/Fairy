## Fairy Demo Frontend (Next.js)

### 运行

1) 复制环境变量文件：

- 从 `env.template` 复制到 `.env.local`
- 确认 `NEXT_PUBLIC_BACKEND_URL` 指向后端，例如：`http://localhost:8000`

2) 安装依赖并启动：

```bash
npm install
npm run dev
```

打开 `http://localhost:3000`。


